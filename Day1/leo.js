function story(hero,sister,guard){
    return `${hero} is the hero of the movie,his sister ${sister} is killed by
    his Father.${guard} is guard for ${hero} family.${guard} helps ${hero}
     to take revenge for ${sister} death.Finally they both defeated his father`
}

module.exports=story;